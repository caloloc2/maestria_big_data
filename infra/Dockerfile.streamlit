# Imagen del tablero Streamlit (placeholder Fase 0; se desarrolla en la Fase 7).
FROM python:3.11-slim
ENV PYTHONUNBUFFERED=1
WORKDIR /app
COPY infra/requirements.streamlit.txt /tmp/req.txt
RUN pip install --no-cache-dir -r /tmp/req.txt
EXPOSE 8501
# El código (src/) se monta como volumen en dev.
