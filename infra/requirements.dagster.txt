# Imagen Dagster — orquestador + UI + procesamiento batch (Fase 2).
dagster==1.9.*
dagster-webserver==1.9.*
dagster-postgres==0.25.*

# Procesamiento batch (Fase 2): Spark + conectores + calidad de datos
pyspark==3.5.3
pandas==2.2.3
pyarrow==17.0.0
pymysql==1.1.1          # lectura del CDR (SOLO LECTURA)
SQLAlchemy==2.0.35
psycopg2-binary==2.9.12 # escritura a la capa servida PostgreSQL
pandera==0.20.4         # validación de esquemas (gobernanza)
python-dotenv==1.0.1
confluent-kafka==2.5.3   # encolar asr.jobs / leer asr.results (Fase 3)
google-generativeai==0.8.3  # análisis de calidad con Gemini (Fase 4)
s3fs==2024.9.0           # escritura de Parquet a MinIO desde pandas (bronze_cdr)
paramiko==3.5.0          # SFTP SOLO LECTURA del audio crudo → Bronce (bronze_audio, streaming)
statsmodels==0.15.0      # pronóstico de KPIs (Fase 7): SARIMA / Holt-Winters (comparación)
scikit-learn==1.9.0      # métricas de pronóstico (MAE/RMSE/R²) y modelos ML (Fase 7/8)
prophet==1.1.6           # pronóstico principal (Fase 7): tendencia + estacionalidad anual/semanal + changepoints
# torch (CPU) se instala aparte en Dockerfile.dagster con el índice CPU (LSTM comparativo, Fase 8)
