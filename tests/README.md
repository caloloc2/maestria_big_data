# `tests/` — Pruebas

Pruebas unitarias y de integración de los módulos de `src/`. Se irán agregando por
fase (p. ej. validación de esquemas `pydantic`, cobertura del cruce CDR↔grabación,
disparo de `venta_valida=0` ante infracción crítica). Framework: `pytest`.
